{
  "name": "ghostprotocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fresh GhostProtocol starter with a runnable Playwright demo.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prestart": "npm run build",
    "start": "node --enable-source-maps dist/index.js"
  },
  "dependencies": {
    "playwright": "^1.47.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
